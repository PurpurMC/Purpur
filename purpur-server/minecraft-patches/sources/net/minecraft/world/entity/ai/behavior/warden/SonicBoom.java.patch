--- a/net/minecraft/world/entity/ai/behavior/warden/SonicBoom.java
+++ b/net/minecraft/world/entity/ai/behavior/warden/SonicBoom.java
@@ -42,7 +_,7 @@
 
     @Override
     protected boolean checkExtraStartConditions(ServerLevel level, Warden owner) {
-        return owner.closerThan(owner.getBrain().getMemory(MemoryModuleType.ATTACK_TARGET).get(), 15.0, 20.0);
+        return owner.level().purpurConfig.wardenCanUseSonicBoom && owner.closerThan(owner.getBrain().getMemory(MemoryModuleType.ATTACK_TARGET).get(), 15.0, 20.0);
     }
 
     @Override
