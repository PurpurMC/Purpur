--- a/net/minecraft/world/clock/ServerClockManager.java
+++ b/net/minecraft/world/clock/ServerClockManager.java
@@ -70,7 +_,17 @@
     public void tick() {
         boolean advanceTime = this.advanceTime(); // Paper - per-world time
         if (advanceTime) {
-            this.clocks.values().forEach(ServerClockManager.ServerClockInstance::tick);
+            // Purpur start - Configurable daylight cycle
+            this.clocks.forEach(((worldClockHolder, clockInstance) -> {
+                ServerLevel serverLevel = this.level != null ? this.level : server.overworld();
+                float incrementTicks = 12000F / (serverLevel.isBrightOutside() ? serverLevel.purpurConfig.daytimeTicks : serverLevel.purpurConfig.nighttimeTicks);
+                float rate = clockInstance.rate;
+                clockInstance.rate *= incrementTicks;
+                clockInstance.tick();
+                clockInstance.rate = rate;
+                this.broadcastUpdates(worldClockHolder, clockInstance);
+            }));
+            // Purpur end - Configurable daylight cycle
             this.setDirty();
         }
     }
